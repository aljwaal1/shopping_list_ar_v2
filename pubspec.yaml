name: shopping_list_ar
description: قائمة المشتريات - Flutter Android APK direct.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
